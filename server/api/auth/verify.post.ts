// import firebase from "~~/server/firebase";

export default defineEventHandler(async (event) => {
    // let result = await firebase.auth().verifyIdToken(event.context.params?.token as string)

    // console.log(result);

    return {
        // uid: result.uid,
    }
})